# Configuración del Backend API
VITE_API_URL=http://localhost:2727/api/v1

# Feature Flag: Habilitar sessionStorage como fallback para el access token
# Por defecto false por seguridad - solo habilitar en desarrollo si es necesario
VITE_ENABLE_SESSION_FALLBACK=false