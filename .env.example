# Configuración del Backend API
VITE_API_URL=http://localhost:3000/api/v1

# NOTA: El token de autenticación ahora se almacena en localStorage
# para mantener la sesión del usuario entre recargas de página y cierres del navegador.
# El token tiene una duración de 30 días, por lo que el usuario no necesita
# autenticarse frecuentemente.